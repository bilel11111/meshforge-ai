# Optional cloud provider key for Gemini-backed generation.
# Leave empty to use procedural generation or a locally detected model.
GEMINI_API_KEY=""

# Optional base URL for a hosted deployment. Desktop builds do not require it.
APP_URL=""
